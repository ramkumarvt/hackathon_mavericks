import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'meeting_room_schedule',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
